{
  "private": true,
  "main": "./cvmanager_server.js",
  "scripts": {
    "start": "npm install && bower install && node cvmanager_server"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "body-parser": "^1.12.4",
    "express": "^4.12.4",
    "fs-extra": "^0.22.1",
    "path": "*",
    "is-there": "*",
    "elasticsearch":"*",
    "node-uuid":"*"
  }
}
